import Taskbar from "../../taskbar";

const ShowDesktop = () => {
  return (
    <Taskbar.Tooltip label="Show desktop">
      <div className="show-desktop">
        <div />
      </div>
    </Taskbar.Tooltip>
  );
};

export default ShowDesktop;
